[
  {
    "query": "verify-event-model just passed for StockReserved in my inventory context. Now I need to create src/Inventory/Stock/Events/StockReserved.hs with the payload type, add the StockReserved variant to src/Inventory/Stock/Event.hs, extend getEventEntityId with a new branch, and add the matching case to the update fold in src/Inventory/Stock/Entity.hs. Walk me through each of those three files.",
    "should_trigger": true
  },
  {
    "query": "how do i add a cartitemadded event to my cart feature? the decide function in my command module references the CartItemAdded constructor but there's no Events/CartItemAdded.hs file yet, so the build fails",
    "should_trigger": true
  },
  {
    "query": "need to implment the event side of the IncrementCounter command — create the CounterIncremented paylod module under Events/, add it to the ADT in Event.hs, and make sure the update fold in Entity.hs handles it corectly",
    "should_trigger": true
  },
  {
    "query": "The red unit test I wrote describes that when BookReturned fires on a LibraryBook entity, the checkedOut field should become False and dueDate should reset to Nothing. I'm now in the GREEN phase and need to write the payload module, update the Event ADT, and extend the update fold to make the test pass.",
    "should_trigger": true
  },
  {
    "query": "Events/CartCheckedOut.hs is listed in .locked-files but I need to add a discountCode field to that event payload. I know I can't edit the locked file directly. What's the correct way to introduce the change — do I create CartCheckedOutV2.hs, and if so what does the in-file type need to be called?",
    "should_trigger": true
  },
  {
    "query": "neo build is giving me a non-exhaustive pattern match warning on getEventEntityId in src/Library/Book/Event.hs — I just added a BookRenewed variant to the ADT but forgot to add the corresponding branch. Also need to add a BookRenewed case to the update fold in Entity.hs. How do I fix both?",
    "should_trigger": true
  },
  {
    "query": "I'm naming a new event for when a counter is first set up. My colleague says 'CounterCreated' is a CRUD smell and I should use something else, but I'm not sure. I also need to actually create the payload module under Events/ and wire it into the Entity update fold. What's the right event name and what are the three files I need to write?",
    "should_trigger": true
  },
  {
    "query": "Adding a StockDepleted event to the inventory bounded context. The event has fields entityId :: Uuid and depletedAt :: Text. Please generate Events/StockDepleted.hs, show the new variant in Event.hs including the updated getEventEntityId, and add the StockDepleted branch to the update fold in Entity.hs — using NeoHaskell types throughout, no String or Data.Aeson.",
    "should_trigger": true
  },
  {
    "query": "I need to implement the decide function for the AddItemToCart command in NeoHaskell. The CartItemAdded event payload module already exists at src/Cart/Events/CartItemAdded.hs. I just need to write the command handler logic that validates the input and emits the correct event — what does a decide function look like and where does it live?",
    "should_trigger": false
  },
  {
    "query": "Before I start coding, I want to verify that my event model for the library checkout flow is sound. The proposed event node is BookBorrowed with fields borrowerId and dueDate on the LibraryBook entity. Does this pass the event-model validation rules — is the name a domain fact, are the fields right, and is the entity assignment correct?",
    "should_trigger": false
  },
  {
    "query": "I've implemented CounterIncremented and updated Entity.hs, but now I realize CounterEntity is missing a lastIncrementedAt field that the business needs. I need to add that field to the entity record, update initialState, and handle the default. How do I expand the entity with a new field in NeoHaskell?",
    "should_trigger": false
  },
  {
    "query": "I need to write unit tests for how CartEntity changes when CartItemAdded fires. Specifically I want to test that entity.itemCount increments by one and entity.totalPrice increases by the item price after the update fold runs. What does a NeoHaskell unit test for the entity update behavior look like?",
    "should_trigger": false
  },
  {
    "query": "I'm in the early design phase for a stock management feature. I'm not sure whether to model 'StockAdjusted' as a single event with a signed delta field, or split it into 'StockIncreased' and 'StockDecreased'. What event-modeling heuristics should guide that decision and how do I pick the right granularity?",
    "should_trigger": false
  },
  {
    "query": "I've finished implementing all the commands and events for the Cart bounded context — CartCreated, CartItemAdded, CartCheckedOut all have payload modules and Entity.hs is fully updated. Now I need to wire the feature so it's actually reachable. Which files do I need to update to register the context with the application router?",
    "should_trigger": false
  },
  {
    "query": "Where exactly should I put the Event.hs and Entity.hs files for a new Library bounded context? I want to make sure I follow the NeoHaskell module layout conventions before I start creating any files — what's the expected directory structure under src/?",
    "should_trigger": false
  },
  {
    "query": "I need to implement a NeoHaskell query that returns the current state of a CartEntity given its id, including projecting over CartCreated and CartItemAdded events. What does the query handler module look like, where does it live, and how does it fold the event stream to produce the entity snapshot?",
    "should_trigger": false
  }
]
